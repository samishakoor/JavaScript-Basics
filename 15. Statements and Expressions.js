// Statements and Expressions


// expressions are those which produce/return something
3 + 4       // an expression
1991         // an expression
true && false && !false       // an expression

// statement is like a bigger piece of code which do not return a value
if (23 > 10) {
  const str = '23 is bigger';         // a statement (we can said a statemant as a declaration)just like a complete sentence
}

const me = 'Jonas';
console.log(`I'm ${2037 - 1991} years old ${me}`);

console.log("I'm " + (2037-1991) + " years old " + me)
