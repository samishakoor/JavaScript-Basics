'use strict';                                       //(to write a secure javaScript code to avoid accidental errors) & must be written on first line

// Activating Strict Mode
let hasDriversLicense = false;
const passTest = true;

if (passTest) hasDriverLicense = true;                    // this line will throw an error (because s is missing in hasDriverLisence ) if we use strict mode bt if we don't , then nothing is displayed because of error
if (hasDriversLicense) console.log('I can drive :D');


// we cannot use reserved keywords of javaScript as a variable

// const interface = 'Audio';
// const private = 534;
//const if=23